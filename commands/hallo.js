const discord = require("discord.js");

module.exports.run = async(bot, message, args) => {
    console.log("halleu");
}

module.exports.help = {
    name: "hallo",
    description: "test command, doesn't show anything"
}